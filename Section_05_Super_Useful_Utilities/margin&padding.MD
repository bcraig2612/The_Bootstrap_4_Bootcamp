<!-- {property}{sides}-{size} for XS -->

<!-- {property}{sides}-{breakpoint}-{size} for SM, MD, LG, XL -->

<!-- Property: 
    m for margin, 
    p for padding 
-->

<!-- Sides: 
    t for top, 
    b for bottom, 
    l for left, 
    r for right, 
    x for left & right, 
    y for top & bottom, 
    blank to set margin or padding for all 4 sides 
-->

<!-- Size:  
    0 - for classes that eliminate margin/padding by setting it to 0
    1 - (by default) for classes that set the margin or padding to $spacer * .25 rems
    2 - (by default) for classes that set the margin or padding to $spacer * .5 rems
    3 - (by default) for classes that set the margin or padding to $spacer
    4 - (by default) for classes that set the margin or padding to $spacer * 1.5 rems
    5 - (by default) for classes that set the margin or padding to $spacer * 3 rems
    auto - for classes that set the margin to auto
    (You can add more sizes by adding entries to the $spacers Sass map variable.)
-->